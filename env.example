# Dimath Group Environment Configuration
# Copy this file to .env and update with your actual values

# Site Configuration
SITE_URL=dimathgroup.lk

# Database Configuration
DB_HOST=localhost
DB_NAME=dimathgroup
DB_USER=root
DB_PASS=
DB_PORT=3306

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=info@dimathgroup.lk
SMTP_PASSWORD=your-app-password
SMTP_ENCRYPTION=tls

# Email Settings
FROM_EMAIL=info@dimathgroup.lk
FROM_NAME=Dimath Group
LEAD_EMAIL_SUBJECT=New Contact Form Submission - Dimath Group

# Application Settings
APP_ENV=production
APP_DEBUG=false